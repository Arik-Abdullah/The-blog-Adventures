# Environment variables for The Blog Adventures application

# Flask Secret Key - Generate a secure random key for production
# You can generate one with: python -c "import os; print(os.urandom(24).hex())"
SECRET_KEY=your_secure_secret_key_here

# Flask Debug Mode - Set to 'true' only in development
FLASK_DEBUG=false

# Database Configuration (if needed in future)
# DATABASE_URL=sqlite:///blog.db